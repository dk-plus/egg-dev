module.exports = app => {
  // app.once('server', () => {
  //   console.log('server')
  // });
  // app.on('error', (error, ctx) => {
  //   console.log('error:', error)
  // });
  // app.on('request', (ctx) => {
  //   // console.log('request:', JSON.stringify(ctx.request, {}, 2));
  //   const {
  //     method, url,
  //   } = ctx.request;
  //   console.log(`[${method}]\t${url}`)
  // });
  // app.on('response', (ctx) => {
  //   // console.log('response:', JSON.stringify(ctx, {}, 4));
  //   console.log('used:', Date.now() - ctx.starttime);
  // });
}